# Runtime/dev dependencies for the lakehouse (silver/gold) batch jobs.
# PySpark ships with the apache/spark image; these pins are for host/CI runs.
pyspark>=4.2.0,<4.3
delta-spark>=3.2,<3.3
great-expectations>=0.18,<1.0
pandas>=2.0,<3.0
numpy>=1.24
pyarrow>=14
scikit-learn>=1.9.1,<2
