# Development & test dependencies (shared across modules).
-r ingestion/requirements.txt

pytest>=9.1.1,<10      # >=9.0.3 fixes PYSEC-2026-1845
ruff>=0.16.8,<1.0
pip-audit>=2.10.1,<3      # dependency CVE gate (see .github/workflows/ci.yml)

# Pure-Python ML/quality/analytics tests (Spark tests skip without pyspark).
pandas>=2.0,<3.0
numpy>=1.24
scikit-learn>=1.9.1,<2
duckdb>=0.10
